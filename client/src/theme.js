import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
    typography: {
        fontFamily: [
          'Shrikhand',
          'cursive',
        ]},
  palette: {
    primary: { 500: '#467fcf' },
  },
})

export default theme